export enum RoutesUrls {
  root = '/',
  home = '/home',
  employees = '/employees',
  businessTrips = '/businessTrips',
  reports = '/reports',
  login = '/login',
  logout = '/logout',
  settings = '/settings',
  notFound = '/not-found',
  structure= '/structure',
  schedule= '/schedule',
}
